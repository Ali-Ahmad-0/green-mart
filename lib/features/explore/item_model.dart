import 'package:flutter/material.dart';

class ItemModel {
  final String image, title;
  final Color color;
  const ItemModel({
    required this.image,
    required this.title,
    required this.color,
  });
}
